import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Contact Tim Hauke",
  description: "Send a message, schedule a consultation, or request access to portfolio assets.",
  alternates: {
    canonical: "/contact",
  },
}
